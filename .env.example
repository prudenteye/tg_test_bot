# Environment variables for Vercel Python Serverless Telegram bot
# NOTE: On Vercel, set these in Project Settings > Environment Variables.
# This file is for documentation/local reference only. Do NOT commit real secrets.

# Telegram Bot token (preferred)
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
# Alternative variable name also supported by code (optional)
# BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# CSV lookup configuration
# Absolute path in the serverless runtime (if the CSV is bundled under api/)
# Example: /var/task/api/data.csv
CSV_FILE_PATH=/var/task/api/data.csv

# Key column name in the CSV; defaults to "id" if not set
CSV_KEY_COLUMN=id