# Environment variables for Vercel Python Serverless Telegram bot (Supabase/Postgres only)
# NOTE: On Vercel, set these in Project Settings > Environment Variables.
# This file is for documentation/local reference only. Do NOT commit real secrets.

# Telegram Bot token (preferred)
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
# Alternative variable name also supported by code (optional)
# BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# Database (Supabase/Postgres) connection string
# Prefer a direct Postgres URL with sslmode=require
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
# Alternatively, you can use SUPABASE_DB_URL if you prefer that env name
# SUPABASE_DB_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Table and search column
SUPABASE_TABLE_NAME=accounts
SUPABASE_SEARCH_COLUMN=account